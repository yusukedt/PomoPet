specVersion: "v1beta"
serviceId: "testapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "testapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
